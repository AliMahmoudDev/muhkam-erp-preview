You are now performing FINAL PRODUCTION HARDENING.

This is NOT optional. The system must be SAFE for real customers.

No shortcuts. No assumptions.

---

## 🔴 TASK 1 — REMOVE ALL warehouse_id FALLBACKS

Search for:
?? 1
|| 1

Specifically for:
warehouse_id

REPLACE:

```ts
warehouse_id ?? 1
```

WITH:

```ts
if (!warehouse_id) {
  return res.status(400).json({ error: "Warehouse must be selected" });
}
```

NEVER default to warehouse 1.

---

## 🔴 TASK 2 — ENFORCE DATABASE CONSTRAINT

Run this SQL immediately:

```sql
ALTER TABLE erp_users
ADD CONSTRAINT erp_users_company_check
CHECK (role = 'super_admin' OR company_id IS NOT NULL);
```

Then verify:

* No invalid rows exist
* Fix any broken data

---

## 🔴 TASK 3 — ENABLE REDIS (MANDATORY)

If NODE_ENV=production:

* REDIS_URL must be present

If missing:

* Server must FAIL to start

No fallback allowed in production.

---

## 🔴 TASK 4 — LOCK RESTORE ENDPOINT HARD

Ensure:

* Only super_admin can access
* Must include Idempotency-Key
* Must verify:

  * company_id matches
  * file not truncated
* Must NOT affect other companies
* Must log:

  * START
  * SUCCESS
  * FAILURE

---

## 🔴 TASK 5 — ADD RUNTIME GUARD (VERY IMPORTANT)

Create middleware:

```ts
function enforceTenant(req, res, next) {
  if (!req.user?.company_id && req.user?.role !== "super_admin") {
    throw new Error("Invalid tenant context");
  }
  next();
}
```

Apply globally to all protected routes.

---

## 🔴 TASK 6 — FINAL GLOBAL SCAN

Search entire project for:

* queries without company_id
* any default fallback values
* any DELETE without tenant filter

Fix ALL of them.

---

## 🔴 TASK 7 — FINAL VALIDATION

Run tests:

1. User without company_id → must fail
2. Cross-tenant access → must fail
3. Restore wrong company → must fail
4. Missing warehouse_id → must fail
5. Redis disabled in production → must crash

---

## OUTPUT

Return:

* All files modified
* All SQL executed
* Final confirmation:

"System is now SAFE for multi-tenant SaaS production"

Or explain why not.
