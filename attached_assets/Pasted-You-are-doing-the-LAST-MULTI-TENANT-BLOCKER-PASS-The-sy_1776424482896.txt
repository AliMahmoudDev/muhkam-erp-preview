You are doing the LAST MULTI-TENANT BLOCKER PASS.

The system is still NOT safe for multi-tenant production until these remaining issues are closed.

Do NOT stop at partial fixes.
Do NOT say "done" if any unsafe route remains.

---

## 🔴 FIX A — CLOSE ALL REMAINING CROSS-TENANT LOOKUPS

Search for ALL places where these tables are accessed by `id` only:

* safesTable
* customersTable
* erpUsersTable
* employeesTable
* warehousesTable
* productsTable
* salesTable
* purchasesTable

For every SELECT / UPDATE / DELETE on tenant-owned tables, enforce:

```ts
where(and(eq(table.id, id), eq(table.company_id, companyId)))
```

Return 404 if ownership check fails.

---

## 🔴 FIX B — REMOVE ALL RELIANCE ON `company_id default(1)`

Search entire backend for inserts into:

* stock_movements
* transactions
* customer_ledger
* any tenant-owned table

Every insert MUST explicitly set:

```ts
company_id: companyId
```

No silent defaults.
No schema fallback.
No omitted company_id.

Also search schema files for:

* `default(1)` on `company_id`
* any tenant table where company_id is nullable or defaulted

Remove those defaults if they still exist.

---

## 🔴 FIX C — ATOMIC STOCK FOR ALL STOCK-CHANGING PATHS

Search all routes for any stock mutation pattern like:

* read quantity
* compare in JS
* update later

Replace with atomic SQL update or row locking.

Specifically audit and fix:

* sales-cancel
* purchases-cancel
* return-cancel
* stock transfer
* inventory adjustments
* any reverse/cancel flow

Use one of:

1. atomic UPDATE ... WHERE quantity >= X
2. SELECT ... FOR UPDATE before mutation

No stock-changing path may remain non-atomic.

---

## 🔴 FIX D — TENANT-SAFE FOREIGN KEY VALIDATION EVERYWHERE

Before persisting any foreign ID from request body:

* safe_id
* customer_id
* employee_id
* warehouse_id
* product_id
* sale_id
* purchase_id

Validate ownership under the caller's company first.
Do not trust request IDs.

---

## 🔴 FIX E — GLOBAL PARANOIA SCAN

Search for all of the following in the backend:

* `.where(eq(`
* `default(1)`
* `company_id:`
* missing `company_id`
* `sql.raw(`
* stock updates
* cancel/reverse flows

List every suspicious occurrence and either:

* prove it is safe, or
* fix it.

Do not skip any file.

---

## 🔴 FIX F — REGRESSION TESTS

Add/extend tests for:

* cross-tenant safe_id injection
* cross-tenant customer_id injection
* cancel sale from another tenant
* cancel purchase from another tenant
* stock race on same product
* inserts failing when company_id omitted
* reverse/cancel flows preserving tenant isolation

---

## OUTPUT REQUIRED

Return ONLY:

1. files modified
2. exact remaining risks, if any
3. verdict:

   * SAFE FOR MULTI-TENANT PRODUCTION
   * SAFE FOR PILOT ONLY
   * NOT SAFE

If any unsafe route remains, say so explicitly.
No praise.
No generic summary.
