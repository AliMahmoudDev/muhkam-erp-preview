You are fixing the GitHub Actions auto-deploy pipeline so deployments become reliable again.

Goal:
Make push-to-main deploy automatically to the VPS successfully and safely.

Do NOT ask me for passwords.
Do NOT use insecure shortcuts.
Use GitHub Secrets and server env only.

==================================================
TASK 1 — FIX THE 4 BLOCKING LINT ERRORS
=======================================

Fix these exact ESLint errors cleanly:

1. artifacts/erp-system/src/pages/attendance.tsx
   Remove unused import: Calendar

2. artifacts/erp-system/src/pages/customers.tsx
   Remove unused variable/function: handleUpdateClassification

3. artifacts/erp-system/src/pages/payroll.tsx
   Remove unused imports: ChevronRight, Settings

Do not disable the lint rule globally.
Do not ignore the errors.
Actually remove or use the code properly.

==================================================
TASK 2 — FIX TEST COVERAGE FAILURE CLEANLY
==========================================

Tests are passing, but coverage threshold is failing.

Adjust the API test coverage config cleanly so CI passes without fake hacks.

Requirements:

* Keep tests running
* Keep coverage enabled
* Lower the global threshold only to a realistic value that passes current suite
* Do NOT disable coverage entirely

Use a practical threshold such as 45% for lines/statements if needed.

Return the exact file changed.

==================================================
TASK 3 — FIX PRODUCTION REDIS ENV IN DEPLOY
===========================================

The app correctly fails in production if REDIS_URL is missing.
So the deploy pipeline must pass REDIS_URL to the server process.

Update the GitHub Actions deploy workflow and the VPS process environment so that:

* REDIS_URL is available during deploy
* pm2 restart uses updated env
* production starts successfully

Use GitHub Secrets for:

* VPS_REDIS_URL
* VPS_DATABASE_URL
* VPS_JWT_SECRET
* VPS_JWT_REFRESH_SECRET

Do not hardcode secrets.

==================================================
TASK 4 — FIX DATABASE PUSH FAILURE SAFELY
=========================================

Current deploy fails at drizzle push because of bad existing data:
categories.company_id points to company_id=1, but company 1 does not exist.

Fix this safely.

Do ALL of the following:

1. Inspect the current DB inconsistency causing:
   categories_company_id_companies_id_fk violation
2. Create a safe repair script or SQL migration that:

   * either creates/fixes the missing company row if that is the intended tenant
   * OR reassigns/deletes orphan categories rows safely after inspection
3. Make the repair explicit and idempotent
4. Ensure future deploys do not fail for the same reason

Do not blindly delete business data unless absolutely necessary.
Prefer a repair script with clear logic.

==================================================
TASK 5 — MAKE DEPLOY WORKFLOW ROBUST
====================================

Update .github/workflows/deploy.yml so the deploy does this safely:

1. SSH into VPS
2. cd into repo
3. git fetch + reset to origin/main
4. export all required env vars:

   * DATABASE_URL
   * REDIS_URL
   * JWT_SECRET
   * JWT_REFRESH_SECRET
   * NODE_ENV=production
5. pnpm install --frozen-lockfile
6. run the DB repair script (if needed)
7. run drizzle push
8. build frontend
9. build api-server
10. pm2 restart halaltech-api --update-env
11. health check /api/healthz
12. fail loudly if any step fails

Also add:
set -euo pipefail

==================================================
TASK 6 — FINAL VERIFICATION
===========================

After fixes, verify all of this:

* lint passes
* tests pass
* coverage passes
* drizzle push passes
* build passes
* pm2 restart succeeds
* /api/healthz returns 200
* GitHub Actions deploy passes end-to-end

==================================================
OUTPUT FORMAT
=============

Return exactly:

1. FILES CHANGED
2. WHAT WAS FIXED
3. ANY REQUIRED GITHUB SECRETS
4. ANY REQUIRED VPS ENV / PM2 CHANGES
5. FINAL DEPLOY WORKFLOW CONTENT
6. FINAL RESULT:

   * AUTO DEPLOY FIXED
   * or BLOCKED with exact remaining blocker
